"""YAML-based configuration loader for Q CTF Agent.

Supplements the existing env-based config.py with an optional YAML
file that provides richer configuration (target auth, focus areas,
parallel settings, etc.).
"""
